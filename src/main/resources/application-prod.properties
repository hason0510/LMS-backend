spring.application.name=backend

spring.datasource.url=${DATASOURCE_URL_PROD}
spring.datasource.username=${DATASOURCE_USERNAME_PROD}
spring.datasource.password=${DATASOURCE_PASSWORD_PROD}
frontend.url=${FRONTEND_URL_PROD}
frontend.allowed-origins=${FRONTEND_ALLOWED_ORIGINS:${FRONTEND_URL_PROD}}
hayson.jwt.refresh-cookie-secure=${JWT_REFRESH_COOKIE_SECURE:true}
hayson.jwt.refresh-cookie-same-site=${JWT_REFRESH_COOKIE_SAME_SITE:None}

server.port=${PORT:8081}



